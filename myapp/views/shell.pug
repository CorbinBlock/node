extends layout

block layout-content
  div.View.UserView
    h1.Banner #{userProfile.nickname}'s command interface
    div.Message
      div.Title
        h3 Execute commands under
        h1 user account:
      pre.Details=JSON.stringify(userProfile, null, 2)
    div.NavButtons
    <script type = "text/javascript" src="execute_shell.js"></script>    
    <input type = "button" onclick = "execShellCommand()" value = "Display">  